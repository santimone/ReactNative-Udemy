import React from 'react';
import { View, Text } from 'react-native';
import { styles } from '../theme/appTheme';

export const ContactsScreen = () => {
	return (
		<View>
			<Text style={{ color: 'white' }}>ContactsScreen</Text>
		</View>
	);
};
